# A sample Gemfile
source "https://rubygems.org"

# requires talib_ruby, indicator, ripl

# run the following to build and install ta-lib first
# ./configure LDFLAGS="-lm"
# make
# sudo make install
# sudo env ARCHFLAGS="-arch x86_64" gem install talib_ruby -- --with-talib-include=~/ta-lib-0.4.0-src/ta-lib/include  --with-talib-lib=/usr/local/lib

gem "talib_ruby"
# gem "rails"
